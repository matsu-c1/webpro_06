<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= data.title %> の編集</title>
</head>
<body>
  <h2><%= data.title %> の編集</h2>

  <form action="/manga/update/<%= id %>" method="POST">
    <table>
      <tr><th>項目</th><th>データ</th></tr>
      <tr>
        <td>作品名</td>
        <td><input type="text" name="title" value="<%= data.title %>"></td>
      </tr>
      <tr>
        <td>作者</td>
        <td><input type="text" name="author" value="<%= data.author %>"></td>
      </tr>
      <tr>
        <td>掲載誌</td>
        <td><input type="text" name="magazine" value="<%= data.magazine %>"></td>
      </tr>
      <tr>
        <td>ジャンル</td>
        <td><input type="text" name="genre" value="<%= data.genre %>"></td>
      </tr>
    </table>

    <input type="submit" value="更新">
  </form>

  <hr>

  <a href="/manga/delete/<%= id %>"
     onclick="return confirm('本当に削除しますか？');">
    削除
  </a>

  <br><br>
  <a href="/manga">一覧に戻る</a>
</body>
</html>