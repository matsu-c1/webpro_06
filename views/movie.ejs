<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>映画一覧</title>
</head>
<body>
  <h2>映画一覧</h2>

  <table>
    <tr>
      <th>ID</th>
      <th>タイトル</th>
      <th>監督</th>
    </tr>

    <% let number = 0 %>
    <% for (let row of data) { %>
      <tr>
        <td><%= row.id %></td>
        <td>
          <a href="/movie/<%= number %>">
            <%= row.title %>
          </a>
        </td>
        <td><%= row.director %></td>
      </tr>
      <% number += 1 %>
    <% } %>
  </table>

  <a href="/movie/create">追加</a>
</body>
</html>
